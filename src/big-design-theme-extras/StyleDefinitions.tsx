import { theme as defaultTheme } from "@bigcommerce/big-design-theme";

// page
export const pageMaxWidth = "1200px"; //taken from boostrap breakpont definition
export const anchorNavWidth = "266px"; // taken from product page
export const tabAnchorBorder = `3px solid ${defaultTheme.colors.primary}`;